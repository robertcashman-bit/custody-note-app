{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Electron: Custody Note",
      "type": "node",
      "request": "launch",
      "cwd": "${workspaceFolder}",
      "runtimeExecutable": "${workspaceFolder}/node_modules/.bin/electron",
      "args": ["."],
      "outputCapture": "std",
      "console": "integratedTerminal"
    }
  ]
}
